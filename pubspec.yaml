name: bluetooth
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+2

environment:
  sdk: ">=2.12.0 <3.0.0"

# consider running `flutter pub upgrade --major-versions`. Alternatively,
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_blue:
    git:
      url: https://github.com/boskokg/flutter_blue/
      ref: 0.11.0
  card_swiper: ^1.0.4
#  permission_handler: ^8.2.2
  another_flushbar: ^1.10.24
#  flutter_bluetooth_serial 0.4.0 // only android
#  bluetooth_low_energy: ^0.1.0   // seems good but not yet, need more investigate
#  flutter_bluetooth_basic: any   // only send data and get paired  error
#  flutter_scan_bluetooth 2.0.2   // just scan
#  flutter_ble_peripheral         // not good
#  flutter_reactive_ble           // interesting need more
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true