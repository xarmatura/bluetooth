import 'package:flutter/material.dart';

class ItemBonded extends StatelessWidget {
  
  const ItemBonded({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
